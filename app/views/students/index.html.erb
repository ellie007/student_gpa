<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>GPA</th>
  </tr>

<% @students.each do |student| %>
  <tr>
    <td><%= student.first%></td>
    <td><%= student.last %></td>
    <td><%= student.average_gpa %></td>
  </tr>
<% end %>
</table>

